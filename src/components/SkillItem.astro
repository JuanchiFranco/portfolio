---
import { Icon } from 'astro-icon/components'
const { name, icon } = Astro.props
---
<div class="flex flex-col items-center">
    {icon && <Icon name={icon} class="w-8 h-8" />}
    <p class="text-[#0096c7] mt-2 text-sm">{name}</p>
</div>